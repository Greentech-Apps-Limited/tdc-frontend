# Base URL for the backend API
NEXT_PUBLIC_API_BASE_URL="https://backend-api-url.com/api"

# API token for secure access
NEXT_PUBLIC_API_TOKEN="API_TOKEN"

# Base URL for the frontend application
NEXT_PUBLIC_APP_URL="https://frontend-url.com"

# Base URL for audio files
NEXT_PUBLIC_AUDIO_URL="https://audio-files-url.com/audio"

# Authentication secret for securing sessions or tokens
AUTH_SECRET="AUTHENTICATION_SECRET"

# Trusted host for authentication (e.g., localhost or production host)
AUTH_TRUST_HOST="http://localhost:8080"
